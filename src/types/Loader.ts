export enum LoadStates {
  // eslint-disable-next-line no-unused-vars
  fetching = 'FETCHING',
  // eslint-disable-next-line no-unused-vars
  fetched = 'FETCHED',
  // eslint-disable-next-line no-unused-vars
  fetchError = 'ERROR'
}
